server:
  port: 9000
  servlet:
    context-path: /emp

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  aop:
    auto: true

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    # 控制台日志配置
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mappers/*.xml




